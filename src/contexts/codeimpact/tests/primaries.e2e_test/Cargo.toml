[package]
name = "codeimpact_primaries_e2e_test"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
codeimpact_hexagon = { path = "../../hexagon" }
codeimpact_secondaries = { path = "../../secondaries" }
serde_json = "1"

[lib]
test = false
doctest = false