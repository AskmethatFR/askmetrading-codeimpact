[package]
name = "codeimpact_secondaries_integration_test"
version = "0.1.0"
edition = "2021"

[dependencies]
codeimpact_hexagon = { path = "../../hexagon" }
codeimpact_secondaries = { path = "../../secondaries" }
serde_json = "1"
tempfile = "3"

[lib]
test = false
doctest = false